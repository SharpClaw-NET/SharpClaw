# External persistence package fixture

This test-only package proves that the production SharpClaw contribution loader can discover an independently packaged persistence provider without a host-side provider switch.
